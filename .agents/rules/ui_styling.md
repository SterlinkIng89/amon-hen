---
description: Global UI Styling Rules
trigger: always_on
---

# UI Styling Rules

- **No Glows**: Do NOT use any glow effects or drop-shadows (e.g., `drop-shadow-*`, `shadow-[0_0_*]`) in the UI components. Stick to clean, flat designs or standard subtle shadows if absolutely necessary.
- **Uppercase Text**: ALL text in the UI must be styled in uppercase. Use CSS or Tailwind's `uppercase` class globally or on elements to ensure this consistency.
